import { SorgulaFilterPipe } from './sorgula-filter.pipe';

describe('SorgulaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SorgulaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
